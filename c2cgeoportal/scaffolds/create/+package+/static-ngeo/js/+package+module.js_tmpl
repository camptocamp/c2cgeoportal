/**
 * @fileoverview This file provides the "app" namespace, which is the
 * application's main namespace. And it defines the application's Angular
 * module.
 */
goog.provide('{{package}}');

/** @suppress {extraRequire} */
goog.require('gmf');


/**
 * @type {!angular.Module}
 */
var {{package}}Module = angular.module('{{package}}', [gmfModule.name]);

/**
 * The default template based URL, used as it by the template cache.
 * @type {string}
 */
{{package}}.baseTemplateUrl = '{{package}}';
