<!DOCTYPE HTML>
<html lang="${lang}">
<head>
    <meta charset="UTF-8">
    <title>API description</title>
    <link rel="shortcut icon" type="image/x-icon" href="${request.static_url('{{package}}:static/images/favicon.ico')}">
    <style type="text/css">
        table.api {
            margin: 10px auto;
            background-color: #ddd;
            border: 3px solid #ddd;
            border-radius: 5px;
        }
        table.api th {
            text-align: center;
            font-weight: bold;
            padding-top: 1px;
            padding-bottom: 3px;
        }
        table.api td.doc {
            background-color: white;
            border: 1px solid lightgrey;
            padding: 10px;
        }
        table.api td.map {
            background-color: transparent;
            padding-top: 3px;
        }
        pre .keyword {
            color: red 
        }
        pre .string, pre .regexp {
            color: orange 
        }
        pre .class, pre .special {
            font-weight: bold 
        }
        pre .number {
            color: blue 
        }
    </style>
</head>
<body>
<table class="api">
    <tr>
        <th>Basis</th>
    </tr>
    <tr>
        <td class="doc">
            To use th API you should add the following HTML:
            <pre>
&lt;script type="text/javascript" src="${request.route_url('apiloader')}"&gt;&lt;/script&gt;
&lt;script&gt;
    window.onload = function() {
        // place to add the commands
    }
&lt;/script&gt;
            </pre>
        </td>
    </tr>
</table>

<table class="api">
    <tr>
        <th>A map</th>
    </tr>
    <tr>
        <td class="doc">
            Code to add:
            <pre class="code">
map1 = new {{package}}.Map({
    div: 'map1',
    zoom: 12,
    easting: 600000,
    northing: 200000,
    height: 400,
    width: 700
});
            </pre>
            Div to add where you want the map: <pre>&lt;div id='map1'&gt;&lt;/div&gt;</pre>
        </td>
    </tr>
    <tr>
        <td class="map"><div id='map1'></div></td>
    </tr>
</table>

<table class="api">
    <tr>
        <th>A map with a marker on its center</th>
    </tr>
    <tr>
        <td class="doc">
            Code to add:
            <pre class="code">
map2 = new {{package}}.Map({
    div: 'map2',
    zoom: 12,
    easting: 600000,
    northing: 200000,
    height: 400,
    width: 700,
    showMarker: true
});
            </pre>
            Div to add where you want the map: <pre>&lt;div id='map2'&gt;&lt;/div&gt;</pre>
        </td>
    </tr>
    <tr>
        <td class="map"><div id='map2'></div></td>
    </tr>
</table>


<table class="api">
    <tr>
        <th>A map with a subset of overlays</th>
    </tr>
    <tr>
        <td class="doc">
            Code to add:
            <pre class="code">
map3 = new {{package}}.Map({
    div: 'map3',
    zoom: 14,
    easting: 663382,
    northing: 244914,
    height: 500,
    width: 700,
    overlays: {
        'local': [
            'Liegenschaften', // example of theme
            'strom_station' // example of layer
        ],
        'external': [
            'Liegenschaften_Ext'
        ]
    }
});
            </pre>
            Div to add where you want the map: <pre>&lt;div id='map3'&gt;&lt;/div&gt;</pre>
        </td>
    </tr>
    <tr>
        <td class="map"><div id='map3'></div></td>
    </tr>
</table>


    <script type="text/javascript" src="${(request.route_url('apiloader') + '?lang=%s%s') % (lang, '&debug' if debug else '')}"></script>
    <script type="text/javascript">
        Ext.select('pre.code').each(function(s) {
            eval(s.dom.innerHTML);
        });
    </script>
    <!-- just for code examples coloration -->
    <script type="text/javascript"
    src="${request.static_url('c2cgeoportal:static/lib/hijs.js')}"></script>
</body>
</html>
