extends: vars_nondocker.yaml

vars:
    checker:
        lang:
            files: [ngeo]
        routes:
            disable: [printproxy_capabilities]

update_paths:
- layers
- checker.lang
- checker.routes
- check_collector.disabled
