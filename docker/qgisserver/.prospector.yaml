inherits:
  - duplicated
  - utils:base
  - utils:fix
  - utils:no-design-checks

pylint:
  options:
    extension-pkg-allow-list: qgis

mypy:
  options:
    python-version: '3.10'

ruff:
  options:
    target-version: py310

pyroma:
  run: false
