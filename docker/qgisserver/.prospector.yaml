inherits:
  - duplicated
  - utils:base
  - utils:fix
  - utils:no-design-checks

pyroma:
  run: False

mypy:
  options:
    python_version: '3.10'

pylint:
  options:
    extension-pkg-allow-list: qgis
