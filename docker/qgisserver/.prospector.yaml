inherits:
  - duplicated
  - utils:base
  - utils:fix
  - utils:no-design-checks

pycodestyle:
  disable:
    - N803 # argument name should be lowercase, done by Pylint

pyroma:
  run: False

bandit:
  options:
    config: .bandit.yaml
