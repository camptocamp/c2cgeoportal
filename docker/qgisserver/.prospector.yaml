inherits:
  - duplicated
  - utils:base
  - utils:fix
  - utils:no-design-checks

pylint:
  options:
    extension-pkg-allow-list: qgis

mypy:
  options:
    python-version: '3.11'

ruff:
  options:
    target-version: py311

pyroma:
  run: false
