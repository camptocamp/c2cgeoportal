---
project_folder: {{project}}
project_package: ${package}
checker_url: https://localhost/${instanceid}/wsgi/c2c/health_check?max_level=9
checker_headers:
  Host: ${host}
managed_files: []
template_vars:
  package: {{package}}
  srid: {{srid}}
  extent: {{extent}}
  apache_vhost: {{apache_vhost}}
